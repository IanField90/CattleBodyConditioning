<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.ianfield.bodyscoring.ViewRecordActivity">

    <item android:id="@+id/action_save_chart"
        android:title="@string/save_chart"
        android:icon="@drawable/download"
        app:showAsAction="ifRoom"
        />

    <!-- TODO show if room when CSV export is complete -->
    <item android:id="@+id/action_upload"
        android:title="@string/save_to_drive"
        android:icon="@drawable/google_drive"
        android:visible="false"
        app:showAsAction="ifRoom"
        />
</menu>
